Put your input video file(s) and labeled action-training `.npz` files here
(both are gitignored by convention - not committed as they're local assets).

- Input video for `--source-type file`, e.g. `data/sample.mp4`.
- Training data recorded by `scripts/collect_action_data.py`, e.g.
  `data/waving_session1.npz`.
